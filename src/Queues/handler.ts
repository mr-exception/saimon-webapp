/**
 * @author mr-exception
 *
 * this methods are going to handle all layer and jobs in project. the retrive
 * jobs from each job and decide with queue is allowed to have his first job
 * to be done. you can check for more information on https://salimon.ir/protocol
 */

// job handlers

export const start = async () => {
  // ReportLayer.start();
};
